{
  "name": "gear-tracker-import-worker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types test/extract.test.mjs && node --experimental-strip-types test/handler.test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.99.0"
  }
}
